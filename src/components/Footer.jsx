// components/Footer.jsx — minimal, already inlined into Contact.js
// This is a passthrough to avoid import errors — the footer is rendered inside Contact.js
import React from 'react';
export default function Footer() {
  return null;
}
